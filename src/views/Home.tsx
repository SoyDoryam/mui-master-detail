import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography variant="h4">Welcome to the MUI Master-Detail App</Typography>
  );
};

export default Home;